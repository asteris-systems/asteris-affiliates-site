---
import Layout from '../layouts/Layout.astro';

// Asteris Affiliates competitor benchmarks (annual cost, billed yearly, 1 site)
const competitors = [
  { name: 'AffiliateWP',     price: 599, url: 'https://affiliatewp.com',     note: 'Industry standard. Same parent as Easy Digital Downloads.' },
  { name: 'SliceWP Pro',     price: 299, url: 'https://slicewp.com',         note: 'Free core + Pro upgrade. Most affordable major rival.' },
  { name: 'Tapfiliate',      price: 890, url: 'https://tapfiliate.com',      note: 'SaaS — recurring forever, your data lives on their servers.' },
  { name: 'YITH Affiliates', price:  80, url: 'https://yithemes.com',        note: 'Bundled with YITH. Limited features; no MLM, no fraud detection.' },
];

const features = [
  { icon: '👥', title: 'Unlimited affiliates',          text: 'No per-affiliate fee. Onboard 10 or 10,000 — same price.' },
  { icon: '🔗', title: 'Vanity /go/ landing pages',     text: 'Affiliates get branded /go/their-handle URLs. Cookie + view tracking built-in.' },
  { icon: '🌳', title: 'MLM / two-tier referrals',      text: 'Optional tier-2 commissions. Affiliates earn from sub-affiliates they recruit.' },
  { icon: '🤖', title: 'AI swipe-copy generator',       text: '16 pre-written marketing snippets + AI variants. Affiliates copy-paste into emails/socials.' },
  { icon: '🛡️', title: 'Fraud detection',               text: 'Per-IP throttling + optional cloud-assist signal sharing across Asteris stores (opt-in, SHA-256 hashed).' },
  { icon: '💳', title: 'Stripe Connect + PayPal auto-payout', text: 'Direct payouts via Stripe Connect or PayPal Payouts API. Schedule weekly/monthly batches. Bank transfer fallback for non-Stripe/PayPal regions.' },
  { icon: '🛒', title: 'Per-product commission rates',  text: 'Override rates per WC product. High-margin items pay higher commissions.' },
  { icon: '📊', title: 'Real-time stats API',           text: 'Affiliate portal + REST API for headless dashboards. Cursor-pagination, transient-cached.' },
  { icon: '🌐', title: 'EDD + Surecart adapters',       text: 'Not just WooCommerce. Drop-in adapters for Easy Digital Downloads and Surecart.' },
  { icon: '🧪', title: 'A/B email testing',             text: 'Variant routing on welcome / approval / payout emails. Open-pixel tracking.' },
  { icon: '🌍', title: 'i18n + WP-CLI',                 text: 'Translation-ready. Full WP-CLI: list / approve / reject / payout / recompute.' },
  { icon: '🔒', title: 'Self-hosted. Your data.',       text: 'No SaaS subscription forever. No data leaves your server unless YOU enable cloud-assist.' },
];
---

<Layout seoSlug="/">
  <slot name="head" slot="head">
    <meta property="og:image" content="/og-home.png" />
  </slot>

  <!-- Hero -->
  <section class="hero">
    <div class="eyebrow">★ Launched 8 Jun 2026 · v1.1.0 · GPL-2.0</div>
    <h1>
      AffiliateWP-grade features.<br />
      <span class="accent">SliceWP</span> pricing. Forever.
    </h1>
    <p class="lead">
      <strong>Self-hosted affiliate program for WooCommerce, EDD, and Surecart.</strong>
      Unlimited affiliates. MLM tier-2. AI swipe-copy. Stripe Connect + PayPal payouts. Cloud-assist fraud detection.
      <strong>$149/yr for 1 site, $299 for 3, $549 for 10.</strong> No SaaS subscription.
    </p>
    <div class="hero-actions">
      <a href="/pricing" class="btn btn-pop btn-large">Get Asteris Affiliates — $149/yr</a>
      <a href="#vs" class="btn btn-outline btn-large">See vs AffiliateWP →</a>
    </div>
    <p class="no-cc">14-day money-back guarantee · Migrate from AffiliateWP / SliceWP in one CLI command · GPL-2.0 licensed</p>
  </section>

  <!-- VS comparison -->
  <section id="vs" class="vs-section">
    <div class="vs-inner">
      <div class="vs-eyebrow">3–9× cheaper than every major rival</div>
      <h2>The price <span class="vs-accent">is</span> the discount.<br />No founder gimmicks. No "limited time."</h2>
      <p class="vs-sub">Tapfiliate is $890/yr SaaS. AffiliateWP is $599/yr. Our regular price is $149/yr for one site. That's the deal — there's no cheaper-now-then-more-later gotcha.</p>
      <div class="vs-grid">
        <div class="vs-card bad">
          <h3>The status quo</h3>
          <div class="subhead">What most WooCommerce stores pay for an affiliate program today.</div>
          <div class="big">$599<span class="big-suffix">+ /year</span></div>
          <ul class="vs-list">
            {competitors.map(c => (
              <li>
                <strong>{c.name}</strong> · ${c.price}/yr · <span class="vs-note">{c.note}</span>
              </li>
            ))}
          </ul>
        </div>
        <div class="vs-card good">
          <h3>Asteris Affiliates · $149/yr</h3>
          <div class="subhead">Same feature set. No SaaS subscription. Your data stays on your server.</div>
          <div class="big">$149<span class="big-suffix"> /year · 1 site</span></div>
          <ul class="vs-list">
            <li>✅ Unlimited affiliates</li>
            <li>✅ MLM / two-tier referrals</li>
            <li>✅ AI swipe-copy generator</li>
            <li>✅ Vanity /go/ landing pages</li>
            <li>✅ Stripe Connect + PayPal API auto-payout + bank transfer</li>
            <li>✅ Cloud-assist fraud detection (opt-in)</li>
            <li>✅ EDD + Surecart adapters (not just WC)</li>
            <li>✅ Real-time stats API + WP-CLI</li>
            <li>✅ Self-hosted forever — no SaaS bill</li>
          </ul>
        </div>
      </div>
    </div>
  </section>

  <!-- Feature grid -->
  <section class="features">
    <div class="features-inner">
      <h2 class="features-heading">Everything in v1.1.0</h2>
      <p class="features-sub">12 modules covering affiliate onboarding, commission tracking, payouts, fraud, growth — and a few things no one else has.</p>
      <div class="features-grid">
        {features.map(f => (
          <div class="feature">
            <div class="feature-icon">{f.icon}</div>
            <h3 class="feature-title">{f.title}</h3>
            <p class="feature-text">{f.text}</p>
          </div>
        ))}
      </div>
    </div>
  </section>

  <!-- Why differentiator -->
  <section class="why-section">
    <div class="why-inner">
      <h2>Three things no one else has</h2>
      <div class="why-grid">
        <div class="why-card">
          <div class="why-num">01</div>
          <h3>MLM / two-tier referrals</h3>
          <p>Optional. Toggle on, affiliates earn from sub-affiliates they recruit. AffiliateWP charges $199/yr extra for this via an add-on. SliceWP doesn't have it.</p>
        </div>
        <div class="why-card">
          <div class="why-num">02</div>
          <h3>AI swipe-copy generator</h3>
          <p>16 pre-written marketing snippets seeded out of the box, plus AI variants generated via the Asteris AI module (from Asteris for WordPress). Affiliates copy-paste into emails / socials / blog posts.</p>
        </div>
        <div class="why-card">
          <div class="why-num">03</div>
          <h3>Cloud-assist fraud detection</h3>
          <p>Opt-in. SHA-256-hashed fraud signals shared across all Asteris stores. New Asteris store benefits from every prior store's blacklist. Privacy-first: no PII ever leaves your site.</p>
        </div>
      </div>
    </div>
  </section>

  <!-- What it is — primary kw "wordpress affiliate plugin" -->
  <section class="what-section">
    <div class="what-inner">
      <h2>The WordPress affiliate plugin that doesn't rent you back your own data</h2>
      <p>Asteris Affiliates is a self-hosted <a href="/best-woocommerce-affiliate-plugin">WordPress affiliate plugin</a> built for WooCommerce, Easy Digital Downloads, and Surecart stores. It runs entirely on your own server, stores every affiliate, commission, payout and click in your own database, and never sends data to a third party unless you explicitly opt in. There's no monthly subscription, no per-affiliate fee, and no per-transaction tax on what your affiliates earn.</p>
      <p>It replaces a small pile of separate plugins and add-ons that most stores stitch together: a tracker, an MLM add-on, a per-product commission add-on, a payout module, an A/B email tool, an admin impersonator and a fraud detector. Asteris ships the lot for $149/yr (Starter, one site). Three sites is $299. Ten sites is $549. <a href="/pricing">See full pricing →</a></p>
      <p>It is, deliberately, the cheapest serious affiliate plugin in the WordPress ecosystem with this much in the box — and it's the only one that pairs cloud-assist fraud detection (opt-in, SHA-256 hashed, zero PII) with a Stripe Connect direct-payout integration that costs 0.25% instead of PayPal's 2% mass-pay fee. <a href="/migrate">If you're moving from AffiliateWP, SliceWP or Tapfiliate</a>, our CLI importer reads their exports and recreates affiliates, referrals and pending commissions in one command.</p>
    </div>
  </section>

  <!-- Who it's for -->
  <section class="who-section">
    <div class="who-inner">
      <h2>Who Asteris Affiliates is built for</h2>
      <div class="who-grid">
        <div class="who-card">
          <h3>WooCommerce stores running an in-house program</h3>
          <p>If you sell anything on WooCommerce — physical, digital, subscription, bundle — Asteris drops in next to WC, picks up orders automatically, attributes commissions on first-click cookies (configurable), and pays affiliates via Stripe Connect, PayPal API, or bank-CSV. HPOS-compatible. Subscription-renewal commissions if you run WooCommerce Subscriptions. Tested on Storefront, Astra, Kadence, GeneratePress, Avada, Divi.</p>
        </div>
        <div class="who-card">
          <h3>EDD shops selling courses, software or downloads</h3>
          <p>The <a href="/docs/integrations/edd">EDD adapter</a> is in core — same admin, same affiliate portal, same payout engine. No $79/yr "EDD add-on" upcharge. If you sell licences, courses or software bundles, Asteris credits commissions on every successful order and reconciles on refunds automatically.</p>
        </div>
        <div class="who-card">
          <h3>Surecart stores wanting an affiliate program at all</h3>
          <p>Until Asteris, Surecart customers had no native affiliate plugin option. The <a href="/docs/integrations/surecart">Surecart adapter</a> hooks Surecart's checkout webhooks and credits commissions in your WP database. If you've been waiting for Surecart affiliates, this is the build that ships them.</p>
        </div>
        <div class="who-card">
          <h3>Multi-cart agencies and stores</h3>
          <p>Run Woo and EDD on the same site? Same plugin, same affiliates, same payouts. The 10-site Agency licence is for agencies running multiple client stores: one licence covers ten. <a href="/pricing">Agency pricing →</a></p>
        </div>
      </div>
    </div>
  </section>

  <!-- FAQ for FAQPage schema (already declared in seo.ts JSON-LD) -->
  <section class="faq-section">
    <div class="faq-inner">
      <h2>Frequently asked questions</h2>
      <details>
        <summary>Is Asteris Affiliates really self-hosted?</summary>
        <p>Yes. Every affiliate, click, commission, payout and email log lives in your own WordPress database (<code>wp_asteris_*</code> tables). No data ever leaves your server unless you explicitly enable cloud-assist fraud detection — and even then only SHA-256 hashed signals (no IPs, no emails, no order data) are exchanged. Cancel your subscription and the plugin keeps working forever; you just stop getting updates.</p>
      </details>
      <details>
        <summary>Does it support WooCommerce Subscriptions?</summary>
        <p>Yes. Renewal commissions are credited on each successful renewal payment, with separate first-order-only vs lifetime-rebill rules per product if you want it. <a href="/docs/integrations/woocommerce">Integration details →</a></p>
      </details>
      <details>
        <summary>What payout methods are supported?</summary>
        <p>Stripe Connect Express (recommended — 0.25% fee, direct to bank), PayPal API auto-batch (2% PayPal fee, OAuth2-authenticated), bank transfer with AU + US bank-CSV export, and manual marking-as-paid. Per-affiliate method preference. Threshold + schedule control: pay weekly when balance exceeds $50, for example. <a href="/docs/payouts/stripe-connect">Payouts docs →</a></p>
      </details>
      <details>
        <summary>How does Asteris compare to AffiliateWP?</summary>
        <p>Asteris ships every paid AffiliateWP add-on (MLM, per-product rates, bank-CSV, A/B emails, impersonation) in core for $149/yr. AffiliateWP's base is $299/yr (one site) and the comparable add-on stack adds $545+/yr on top. We don't comment on AffiliateWP's longevity — we just out-feature it on price. <a href="/affiliatewp-vs-asteris">Full comparison →</a></p>
      </details>
      <details>
        <summary>Can I try it before I buy?</summary>
        <p>Three ways. <a href="/demo">Launch the Playground demo</a> — a full WP + WC + Asteris store in your browser, no install, no signup, all 31 modules active. Install the <a href="/free">Asteris Affiliates Free</a> tier on your real store (4 core features, up to 25 affiliates). Or buy paid with the 14-day money-back guarantee — no questions asked.</p>
      </details>
    </div>
  </section>

  <!-- Free tier teaser -->
  <section class="free-section">
    <div class="free-inner">
      <h2>There's a free tier too</h2>
      <p>If you have under 25 affiliates and don't need MLM / AI swipe / cloud fraud / Stripe Connect or PayPal auto-payout, the <strong>free version</strong> covers core affiliate tracking + commissions + manual payouts. <a href="/free">See what's in free →</a></p>
    </div>
  </section>

  <!-- CTA -->
  <section class="cta-section">
    <div class="cta-inner">
      <h2>Ready to stop paying SaaS rent?</h2>
      <p>One-time licence for the year. No per-affiliate fees. No data lock-in. Migrate from AffiliateWP / SliceWP / Tapfiliate in one CLI command.</p>
      <a href="/pricing" class="btn btn-pop btn-large">Get Asteris Affiliates →</a>
    </div>
  </section>
</Layout>

<style>
  /* Banner removed — no founder mechanic for Affiliates */

  .hero { max-width: var(--max-w); margin: 0 auto; padding: 96px 24px 64px; text-align: center; }
  .eyebrow { font-size: 14px; letter-spacing: 0.12em; text-transform: uppercase; color: var(--c-muted); margin-bottom: 24px; }
  .hero h1 { font-size: clamp(40px, 6vw, 72px); font-weight: 900; line-height: 1.05; margin-bottom: 24px; letter-spacing: -0.02em; }
  .hero h1 .accent { color: var(--c-mint); }
  .hero .lead { font-size: 19px; max-width: 760px; margin: 0 auto 32px; color: var(--c-text); line-height: 1.55; }
  .hero-actions { display: flex; gap: 16px; justify-content: center; flex-wrap: wrap; margin-bottom: 20px; }
  .no-cc { font-size: 13px; color: var(--c-muted); }

  .vs-section { background: #fff; padding: 96px 24px; border-top: 1px solid var(--c-border); }
  .vs-inner { max-width: var(--max-w); margin: 0 auto; }
  .vs-eyebrow { font-size: 13px; letter-spacing: 0.12em; text-transform: uppercase; color: var(--c-mint); margin-bottom: 16px; font-weight: 700; text-align: center; }
  .vs-section h2 { font-size: clamp(28px, 4vw, 44px); text-align: center; margin-bottom: 16px; line-height: 1.15; }
  .vs-section h2 .vs-accent { color: var(--c-mint); }
  .vs-sub { text-align: center; color: var(--c-muted); max-width: 720px; margin: 0 auto 48px; font-size: 17px; }
  .vs-grid { display: grid; grid-template-columns: 1fr 1fr; gap: 32px; }
  @media (max-width: 768px) { .vs-grid { grid-template-columns: 1fr; } }
  .vs-card { padding: 40px 32px; border-radius: 12px; border: 2px solid var(--c-border); }
  .vs-card.bad { background: #fafafa; }
  .vs-card.good { background: #fff; border-color: var(--c-mint); box-shadow: 6px 6px 0 var(--c-mint); }
  .vs-card h3 { font-size: 22px; margin-bottom: 6px; }
  .vs-card .subhead { color: var(--c-muted); font-size: 14px; margin-bottom: 16px; }
  .vs-card .big { font-size: 56px; font-weight: 900; margin-bottom: 24px; line-height: 1; }
  .vs-card .big-suffix { font-size: 18px; color: var(--c-muted); font-weight: 600; }
  .vs-list { list-style: none; padding: 0; }
  .vs-list li { padding: 10px 0; border-bottom: 1px solid var(--c-border); font-size: 15px; }
  .vs-list li:last-child { border-bottom: 0; }
  .vs-note { color: var(--c-muted); font-size: 13px; }

  .features { padding: 96px 24px; background: var(--c-bg); }
  .features-inner { max-width: var(--max-w); margin: 0 auto; }
  .features-heading { font-size: clamp(28px, 4vw, 40px); text-align: center; margin-bottom: 12px; }
  .features-sub { text-align: center; color: var(--c-muted); max-width: 680px; margin: 0 auto 56px; font-size: 17px; }
  .features-grid { display: grid; grid-template-columns: repeat(auto-fit, minmax(280px, 1fr)); gap: 24px; }
  .feature { background: #fff; border: 1px solid var(--c-border); border-radius: 10px; padding: 28px 24px; }
  .feature-icon { font-size: 32px; margin-bottom: 12px; }
  .feature-title { font-size: 17px; font-weight: 700; margin-bottom: 8px; }
  .feature-text { font-size: 14px; color: var(--c-muted); line-height: 1.5; }

  .why-section { padding: 96px 24px; background: #fff; }
  .why-inner { max-width: var(--max-w); margin: 0 auto; }
  .why-section h2 { font-size: clamp(28px, 4vw, 40px); text-align: center; margin-bottom: 56px; }
  .why-grid { display: grid; grid-template-columns: repeat(auto-fit, minmax(300px, 1fr)); gap: 32px; }
  .why-card { padding: 32px; border-left: 4px solid var(--c-mint); background: var(--c-bg); border-radius: 4px; }
  .why-num { font-size: 14px; color: var(--c-mint); font-weight: 800; letter-spacing: 0.12em; margin-bottom: 8px; }
  .why-card h3 { font-size: 22px; margin-bottom: 12px; }
  .why-card p { color: var(--c-text); line-height: 1.6; }

  .free-section { padding: 64px 24px; background: var(--c-bg); }
  .free-inner { max-width: 760px; margin: 0 auto; text-align: center; }
  .free-section h2 { font-size: clamp(24px, 3vw, 32px); margin-bottom: 16px; }
  .free-section p { font-size: 17px; color: var(--c-text); line-height: 1.6; }
  .free-section a { color: var(--c-mint); font-weight: 700; }

  .what-section { padding: 96px 24px; background: #fff; border-top: 1px solid var(--c-border); }
  .what-inner { max-width: 820px; margin: 0 auto; }
  .what-section h2 { font-size: clamp(28px, 4vw, 38px); margin-bottom: 24px; line-height: 1.2; }
  .what-section p { font-size: 17px; line-height: 1.65; color: var(--c-text); margin-bottom: 18px; }
  .what-section a { color: var(--c-mint); font-weight: 600; }

  .who-section { padding: 96px 24px; background: var(--c-bg); }
  .who-inner { max-width: var(--max-w); margin: 0 auto; }
  .who-section h2 { font-size: clamp(28px, 4vw, 38px); margin-bottom: 36px; text-align: center; }
  .who-grid { display: grid; grid-template-columns: repeat(auto-fit, minmax(320px, 1fr)); gap: 24px; }
  .who-card { background: #fff; padding: 28px; border-radius: 10px; border: 1px solid var(--c-border); }
  .who-card h3 { font-size: 18px; margin-bottom: 12px; color: var(--c-mint); }
  .who-card p { font-size: 15px; line-height: 1.6; color: var(--c-text); }
  .who-card a { color: var(--c-mint); font-weight: 600; }

  .faq-section { padding: 96px 24px; background: #fff; }
  .faq-inner { max-width: 820px; margin: 0 auto; }
  .faq-section h2 { font-size: clamp(28px, 4vw, 38px); margin-bottom: 28px; text-align: center; }
  .faq-section details { border: 1px solid var(--c-border); border-radius: 6px; margin-bottom: 10px; background: var(--c-bg); }
  .faq-section summary { padding: 18px 22px; font-weight: 700; font-size: 16px; cursor: pointer; }
  .faq-section summary:hover { color: var(--c-mint); }
  .faq-section details[open] summary { border-bottom: 1px solid var(--c-border); }
  .faq-section details p { padding: 16px 22px 20px; font-size: 15.5px; line-height: 1.6; color: var(--c-text); }
  .faq-section details a { color: var(--c-mint); font-weight: 600; }
  .faq-section code { background: rgba(0,0,0,0.06); padding: 1px 5px; border-radius: 3px; font-family: var(--font-mono); font-size: 13px; }

  .cta-section { background: var(--c-black); color: #fff; padding: 96px 24px; }
  .cta-inner { max-width: 720px; margin: 0 auto; text-align: center; }
  .cta-section h2 { font-size: clamp(28px, 4vw, 44px); color: #fff; margin-bottom: 16px; }
  .cta-section p { color: rgba(255,255,255,0.85); font-size: 17px; margin-bottom: 32px; line-height: 1.6; }
</style>
